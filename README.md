# smartats-resume-analyzer
 SmartATS is a rule-based ATS resume analyzer that evaluates resumes using real recruiter logic such as skill extraction, job description matching, ATS scoring, and PDF report generation — without using AI models.
